<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="18dp"
    android:background="@drawable/bg_gradient"
    tools:context=".MenuActivity">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="38dp"
        android:gravity="center_horizontal"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="25sp"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="40dp"
        android:padding="12dp"
        >

        <Button
            android:id="@+id/btnNewMuestra"
            android:drawableLeft="@drawable/ic_add_box"
            android:text=" TOMAR NUEVA MUESTRA"
            style="@style/MenuBotton"
            />
        <Button
            android:id="@+id/btnshowMuestras"
            android:drawableLeft="@drawable/ic_publish"
            android:text=" Ver Muestras Publicadas"
            style="@style/MenuBotton"
            />

        <Button
            android:id="@+id/btnLogout"
            android:drawableLeft="@drawable/ic_power_off"
            android:text=" CERRAR SESION"
            style="@style/MenuBotton"
            />


    </LinearLayout>
</LinearLayout>